// Proxy to ui/MetricCard so feature code can use either import path.
// The canonical implementation lives in @/components/ui/MetricCard.
export { MetricCard } from '@/components/ui/MetricCard'
export type { MetricCardProps } from './MetricCard.types'
