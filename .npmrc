# pnpm config for atlas-platform-ui
#
# shamefully-hoist=true is required because Chakra UI v2 and @emotion/* rely on
# peer-dependency resolution that pnpm's strict isolated node_modules breaks.
# Without this, emotion's runtime cache key mismatches cause style conflicts.
shamefully-hoist=true
